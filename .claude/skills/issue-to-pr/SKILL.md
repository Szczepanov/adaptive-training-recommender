---
name: issue-to-pr
description: Implement a GitHub issue end-to-end from its number — read issue plus history, analyze docs and code, write a plan, implement it, verify docs and repo checks, and open a linked PR. Use when the user gives a GitHub issue number to implement.
---

# Issue to PR (Claude Code entry point)

This is a pointer so Claude Code discovers the shared skill. The single source of truth is
[`.agents/skills/issue-to-pr/SKILL.md`](../../../.agents/skills/issue-to-pr/SKILL.md).

Read that file in full now and follow it exactly. Do not copy its contents here — edit the
shared file so every agent stays in sync.
