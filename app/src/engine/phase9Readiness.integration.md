Phase 9.4 intentionally wires the baseline only into the current-date recommendation. Forecast dates require their own exclusive history boundary and are not approximated by reusing today's baseline. The production subjective-drift selector remains off; the integration regression separately proves that the same baseline is observable by the pure evaluator when the experimental selector is explicitly enabled.